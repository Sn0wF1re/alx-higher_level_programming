#!/usr/bin/node
/* prints 3 lines using an array of strings and loop */
const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}
